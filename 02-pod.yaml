apiVersion: v1
kind: Pod
metadata:
  name: nginx #pod name
  labels:
    project: roboshop
    environment: dev
    purpose: poc
spec: # specifications
  containers:
  - name: nginx # contianer name
  - image: nginx # by defauld it will get from docker_hub